# SPEC_PROCESS.md — Brainstorming 过程记录

## 概述

本文档记录了 Mini Coding Agent Harness 项目从需求分析到设计完成的完整 brainstorming 过程。按照 Superpowers 的 brainstorming 技能流程，共经历 7 个阶段。

---

## 第 1 步：明确项目目标

### 讨论内容
- 用户初始描述：项目类型为 "Coding Agent Harness"（南京大学 AI4SE 课程期末项目）
- AI 提出澄清问题：
  1. Harness 的具体定位（评测 vs 运行框架 vs 两者兼顾）
  2. 核心交付物
  3. 是否已有参考项目
  4. 成功标准

### 用户回答
- **定位**：轻量级 Agent 运行框架，管理 Coding Agent 的任务执行流程
- **交付物**：核心系统 + Web UI + CLI + Mock 测试 + 文档 + Docker
- **核心痛点**：Agent 行为不可预测、缺少反馈闭环、缺少执行记录、难以测试验证
- **技术栈**：Python + FastAPI + pytest + Docker

### AI 建议
- 建议通过表格总结关键信息便于确认

### 用户决策
- ✅ 采纳表格式确认
- 确认理解无误

---

## 第 2 步：明确目标用户

### 讨论内容
- AI 提出三个问题：
  1. 主要使用者类型（研究人员 / 开发者 / 学生）
  2. 技术水平
  3. 最关心的三个事情

### 用户回答
- **主要使用者**：开发和维护 Coding Agent 的软件开发者
- **次要使用者**：AI4SE 课程学习者或研究者
- **技术水平**：具有软件开发经验（熟悉编程、Git、测试、LLM API）
- **最关心的三个事情**：
  1. 可靠性（不无限循环、按流程执行）
  2. 可观察性（知道 Agent 为何做出某个决定）
  3. 可扩展性（未来可替换 LLM、增加工具、修改策略）

### AI 建议
- 建议用表格总结用户画像

### 用户决策
- ✅ 确认用户画像清晰

---

## 第 3 步：分析真实使用场景

### 讨论内容
- AI 要求描述 2-3 个端到端使用场景
- 提供参考格式：场景 A：自动修复单文件 Bug

### 用户回答
三个场景：

**场景 A：自动修复单文件 Bug**
- 提供项目代码路径、Bug 描述、测试命令
- Agent：分析 → 修改 → 测试 → 反馈 → 再修改闭环
- Harness：管理完整闭环，不直接生成代码

**场景 B：根据需求实现新功能**
- 提供功能描述和项目仓库
- Agent：分析结构 → 制定计划 → 编写测试 → 实现 → 运行测试
- Harness：保证按工程流程执行，代码可验证

**场景 C：Agent 失败诊断与人工接管**
- Agent 多次尝试失败后 Harness 暂停
- Web UI 展示思考过程、修改历史、错误日志
- 用户决定：提供额外信息 / 修改需求 / 人工接管

### AI 建议
- 将三个场景的核心流程和价值用表格对比

### 用户决策
- ✅ 确认场景覆盖完整

---

## 第 4 步：确定项目范围

### 讨论内容
- AI 要求用 "必须有 / 可以有 / 先不做" 三个分类划分功能

### 用户回答

**必须有（MVP 核心能力）**：
1. Agent 执行循环（Agent Loop）
2. LLM 抽象层（支持 Mock LLM）
3. Tool 系统（读文件、写文件、执行命令、测试）
4. Feedback 循环（收集分析结果、判断成功/失败）
5. 状态管理（SQLite 持久化）
6. 基础 Web UI
7. 安全配置（API Key 不硬编码）
8. 测试体系（pytest + Mock LLM）

**可以有（增强功能）**：
1. 多 Agent 协作
2. Git 自动管理
3. 更丰富工具
4. Agent 性能分析
5. 复杂权限控制

**先不做（明确边界）**：
1. 不做通用 AI 程序员（不替代 Claude Code / Cursor / Copilot）
2. 不做大规模 Benchmark 平台
3. 不训练自己的模型
4. 不支持所有编程语言（仅 Python）
5. 不实现复杂 IDE 插件

### AI 建议
- 无特殊建议，用户范围划分清晰

### 用户决策
- ✅ 确认范围边界

---

## 第 5 步：讨论核心功能模块

### 讨论内容
- AI 要求拆解 8 个核心模块的职责、接口和协作关系

### 用户回答

**8 个核心模块**：
1. **Agent Loop** — 控制执行流程（核心）
2. **Feedback Analyzer** — 分析执行结果
3. **LLM Provider** — 统一模型抽象层
4. **Tool Executor** — 工具执行层
5. **State/Memory Manager** — 状态管理
6. **Task Manager** — 任务管理
7. **Logger** — 可观察性日志
8. **Web UI** — 用户界面

用户重点设计了 Agent Loop 和 Feedback Analyzer 的接口，并绘制了完整的数据流图。

### AI 建议
- 确认模块设计清晰，数据流向合理

### 用户决策
- ✅ 确认 8 模块拆分方案

---

## 第 6 步：讨论技术选型

### 讨论内容
- AI 针对 6 个技术点提问：LLM Provider、Tool Executor、State/Memory、Web UI、Logger、CLI

### 用户回答

| 模块 | 选型 | 理由 |
|------|------|------|
| LLM Provider | 自定义接口 + litellm | 不绑定供应商，Mock 可测试 |
| Tool Executor | asyncio subprocess | 异步非阻塞，适合 Web 服务 |
| State | SQLite | 需要持久化，Web UI 查询 |
| Web UI | FastAPI + React/Vue | 前后端分离，交互能力强 |
| Logger | structlog | 结构化日志，便于分析和展示 |
| CLI | Typer | 类型提示驱动，开发体验好 |

### AI 建议
- 无特殊建议，选型理由充分

### 用户决策
- ✅ 确认全部技术选型

---

## 第 7 步：发现潜在风险

### 讨论内容
- AI 要求按技术、项目、设计、安全四个维度分析风险

### 用户回答

**7 个风险**：
1. **范围失控** — 避免变成通用 AI 编程助手
   - 应对：严格限制 MVP 范围
2. **核心机制不突出** — 避免变成 "调用 LLM API + Web 界面"
   - 应对：自己实现 Agent 状态循环、Tool 调用协议、Feedback 处理
3. **Agent 行为不可预测** — LLM 输出随机性
   - 应对：最大步数限制、权限限制、状态检查、人工介入
4. **真实 LLM 调用导致测试困难** — 不稳定、有成本
   - 应对：Mock LLM 测试，真实 LLM 仅用于演示
5. **任务环境隔离** — 可能污染宿主机
   - 应对：独立 workspace
6. **SPEC 模糊导致 Agent 实现偏离** — 需求理解不一致
   - 应对：明确接口定义、数据结构、验收标准
7. **工程交付不完整** — 遗漏课程要求
   - 应对：从开始维护 PLAN.md、AGENT_LOG.md，最终验收检查

### AI 建议
- 风险 2 最关键：建议先用 Mock LLM + pytest 写端到端测试验证核心机制
- 风险 6：建议在 PLAN.md 中预先定义接口签名
- 风险 7：建议建 CHECKLIST.md

### 用户决策
- ✅ 确认全部风险
- ✅ 在后续文档中采纳 AI 建议（添加 CHECKLIST.md）

---

## AI 提出的重要建议汇总

| # | 建议 | 采纳状态 | 说明 |
|---|------|---------|------|
| 1 | 用表格确认关键信息 | ✅ 采纳 | 用于总结目标用户和场景 |
| 2 | 用 "必须有/可以有/先不做" 分类范围 | ✅ 采纳 | 范围划分清晰 |
| 3 | 先用 Mock LLM + pytest 验证核心机制 | ✅ 采纳 | 体现在 PLAN.md 测试先行 |
| 4 | PLAN.md 预定义接口签名 | ✅ 采纳 | 体现在 Task 定义的实现要点 |
| 5 | 建 CHECKLIST.md 跟踪交付 | ✅ 采纳 | 已创建 |
| 6 | 表格对比三个场景 | ✅ 采纳 | 第 3 步总结 |

## 用户修改或拒绝的建议

所有 AI 建议均被采纳，无修改或拒绝记录。

## 对 Brainstorming 技能效果的反思

### 优点
1. **分步递进**：7 步流程从目标 → 用户 → 场景 → 范围 → 模块 → 技术 → 风险，逻辑完整，没有遗漏
2. **确认机制**：每步结束时 AI 主动要求用户确认，防止理解偏差
3. **引导提问**：AI 在每步开始时提出具体问题，降低用户的启动成本
4. **边界明确**：第 4 步 "明确不做什么" 对小型项目尤为重要，有效防止范围膨胀

### 改进点
1. 第 2 步（目标用户）和第 3 步（使用场景）可以合并或缩短，两者信息高度重叠
2. 第 6 步（技术选型）在第 1 步已有初步答案的情况下，可以更快进入细节讨论

### 总体评价
Brainstorming 技能有效地将模糊的 "Coding Agent Harness" 概念转化为具体的模块划分、技术选型和风险应对方案。整个过程中用户主导需求决策，AI 提供结构化引导和建议，符合 Superpowers 的协作原则。

---

## 补充：SPEC/PLAN 可行性评审（2026-07-08）

在开始实现前，TREA SOLO对现有设计进行工程可行性评估。

### 设计一致性检查

| 模块 | 设计完整性 | 可行性 | 备注 |
|------|------------|--------|------|
| LLM Provider 抽象层 | ✅ 完整 | ✅ 可行 | 接口定义清晰，Mock 设计符合可测试要求 |
| Tool 系统 | ✅ 完整 | ✅ 可行 | workspace 路径检查能有效防范路径遍历 |
| Feedback Analyzer | ✅ 完整 | ✅ 可行 | 基于 exit_code 的分析足够明确、客观 |
| SQLite 状态管理 | ✅ 完整 | ✅ 可行 | 使用标准库，无需额外服务 |
| Agent Loop 状态机 | ✅ 完整 | ✅ 可行 | DECIDE → ACT → OBSERVE 循环逻辑清晰 |
| FastAPI + REST API | ✅ 完整 | ✅ 可行 | 依赖成熟框架 |
| Web UI | ✅ 骨架完整 | ✅ 可行 | 简单静态页面足够满足 MVP 需求 |
| CLI (Typer) | ✅ 完整 | ✅ 可行 | 入口点配置正确 |
| 安全设计 | ✅ 完整 | ✅ 可行 | API Key 掩码、路径隔离、执行边界都覆盖了 |

### 发现并修正的设计困惑

#### 困惑 1：Tool 调用结果格式

**原设计**：PLAN 中提到 `ToolResult` 包含 stdout, stderr, exit_code, success。
**调整**：补充 `duration_ms` 字段记录执行耗时，便于性能分析和超时判断。

#### 困惑 2：Agent 决策解析

**原设计**：SPEC 附录 C 给出了 JSON 格式 `{"tool": "...", "params": {...}}`，但没有说明解析失败的处理。
**补充**：增加解析失败 → 返回结构化错误 → 将错误作为反馈回灌给 LLM → LLM 修正格式。

#### 困惑 3：Workspace 隔离边界

**原设计**：SPEC 6.3 提到 "默认禁止访问 workspace 之外的路径"。
**补充**：实现时将路径解析为绝对路径后检查是否以 workspace 路径为前缀，如果不是则拒绝执行。

#### 困惑 4：反馈循环的终止条件

**原设计**：SPEC 提到 "测试通过后返回结果"。
**补充**：明确两种终止条件：
1. LLM 主动返回 `{"done": true, "summary": "..."}` 表示任务完成
2. 测试结果成功（exit_code=0）且 LLM 确认完成

### 依赖关系验证

依赖图 `Phase 0 → Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5` 正确，没有循环依赖。

并行任务设计合理：
- Phase 0（基础设施）三个任务可并行
- Phase 1（LLM/Tool/Feedback）可并行
- Phase 2（State/Task）可并行
- Phase 4（API/UI/CLI）可并行
- Task 3.1（Agent Loop）确实需要等所有前置依赖就绪

### 验收标准对齐课程要求

课程要求（来自题目）：

| 课程要求 | 设计中是否覆盖 | 位置 |
|----------|----------------|------|
| 自己实现 agent 主循环 | ✅ | SPEC 附录 B，PLAN Task 3.1 |
| LLM 抽象层支持 mock | ✅ | SPEC F-02，PLAN Task 1.1 |
| 机制必须是代码（不是提示词） | ✅ | Feedback = 代码分析器，Guardrail = 代码拦截器 |
| 危险动作拦截可单元测试 | ✅ | Tool 层面的路径检查，可独立测试 |
| 反馈闭环可单元测试 | ✅ | Feedback Analyzer 可独立测试 |
| mock LLM 下机制可测试 | ✅ | 所有核心测试使用 Mock LLM |
| 六个维度都有最低实现 | ✅ | 决策/工具/记忆/治理/反馈/配置都覆盖 |
| 一个维度重点深入 | ✅ 选择反馈闭环 | Feedback Analyzer 深度实现失败分类 |

### 结论

- SPEC.md 和 PLAN.md **设计完整、一致、可行**，可以开始实现
- 上述补充的细节已经明确，实现时遵循即可
- 不需要重大设计变更